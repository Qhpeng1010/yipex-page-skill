# 数字资产行情查询列表评审记录

## 交付结论

- 使用标准 `list.query-table` Recipe 与 `yipex-standard-query-table-v1` 渲染器生成。
- 页面采用标题、四项查询条件、结果标题、Table 和 Pagination 的平面列表结构。
- 页面标题下未声明 `subtitle`，不显示无信息增量的副描述。
- Shell 导航按需求裁剪为“首页、数字资产行情”，未展示无关目录。
- 未声明或调用任何后端接口；行情、状态和更新时间均为浏览器内演示数据。

## 需求覆盖

| 需求 | 实现 | 结果 |
| --- | --- | --- |
| 数字货币场景 | 使用数字资产基础行情查询场景，展示币种、资产类型、网络、参考价格、涨跌幅和状态 | 已覆盖 |
| 查询列表 | 支持关键词、资产类型、网络、交易状态查询以及重置和分页 | 已覆盖 |
| 遵循 YiPex 规则 | 使用标准 Recipe、官方 Ant Design 组件、四列查询网格、全宽 Table 和局部分页 | 已覆盖 |
| 不显示副描述 | `page.root.props` 仅声明 `title` 与 `resultTitle`，没有 `subtitle` | 已覆盖 |
| 不虚构接口 | 所有数据均标记为演示数据，未配置接口地址、请求方法或真实行情来源 | 已覆盖 |

## 已执行自动检查

- Page Spec V2、Page Contract、组件、操作、状态、规则引用与标准 Recipe 一致性检查：通过。
- `preview.html` 构建：通过。
- 页面根节点与生成脚本语法检查：通过。
- 派生预览配置检查：通过，确认 `props` 中不存在 `subtitle`。
- 工作区差异格式检查：通过。

## 待人工验收

- 确认页面标题下没有副描述，并检查标题到查询区的垂直间距是否符合预期。
- 在桌面与窄屏下确认四项筛选、操作按钮、表格横向滚动和分页没有重叠。
- 实际操作查询、重置和分页，确认筛选结果及空状态符合测试预期。
- 生产接入前由业务方确认数字资产对象、币种范围、状态口径、价格精度、行情来源和更新时间语义。

## 验收边界

本次未执行浏览器自动点击、截图、像素比较或真实接口测试。视觉效果、跨设备表现、业务口径和真实数据查询均保持为待人工验收。
