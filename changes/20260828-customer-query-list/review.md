# Review

## 自动检查

- Page Spec V2 结构、Page Contract、区域、操作、状态与交互映射：通过。
- `preview.html` 由 `page-spec.json` 派生并包含 `#yipex-page` 根节点：通过。
- 页面内联 JavaScript 语法：通过。
- 页面未声明或调用后端接口；查询、分页、详情和导出均为浏览器内演示行为。

## 功能覆盖

- 筛选：客户名称、客户编号、客户等级、客户状态、注册日期。
- 结果：客户编号、客户名称、客户等级、客户状态、联系人、联系电话、注册日期、客户经理。
- 操作：重置、查询、分页、导出当前结果、查看右侧详情抽屉。
- 状态：查询 loading、空结果、加载错误、无权限。

## 待人工验收

- 桌面宽度下前四项筛选位于首行，第五项与右对齐操作组位于第二行，结果表格和分页布局清晰。
- 手机宽度下单列筛选、表格横向滚动和详情抽屉宽度合理。
- 组合筛选、重置、分页及无结果反馈符合预期。
- CSV 导出内容不包含操作列，中文字段可正常打开。
- “详情”文字操作和整行点击均能打开抽屉，关闭后保留列表上下文。
- loading、empty、error、permission-denied 四种状态的视觉与交互体验。
